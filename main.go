package main

import (
	. "goprogramming/arrays"
	. "goprogramming/strings"
)

func main() {

	// Beautiful strings
	if false {
		BeautifulStrings(2, "")
	}

	// Pawn Jumps
	if false {
		PawnJumps([]int{2, 3, -1, 1, 3})
	}

	//K Complimentary pairs
	if false {
		ComplimentaryPairs([]int{4, 2, 3, 3, 4, 6, 7, -1, 5, 1, 0}, 6)
	}

}
